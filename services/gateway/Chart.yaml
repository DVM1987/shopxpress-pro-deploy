apiVersion: v2
name: gateway
description: Lab A++ gateway BFF (stateless, Internet-facing, proxies to products and orders)
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: lib
    version: 0.1.0
    repository: file://../../_library
