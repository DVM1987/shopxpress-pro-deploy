# Dev override — bot auto-bumps image.tag after every successful build-push.

image:
  tag: sha-f807f6c

replicas: 1

# 0.7.5b workaround: data layer chưa làm, AWS Secret /shopxpress-pro/dev/products
# chưa tồn tại → ExternalSecret SecretSyncedError. Tạm tắt để App Healthy.
# Khi 0.7.5b xong, đổi enabled: true.
externalSecret:
  enabled: false
  awsSecretPath: /shopxpress-pro/dev/products

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi
