# Stg override — manual PR promotion from dev.

replicas: 0

externalSecret:
  awsSecretPath: /shopxpress-pro/stg/orders

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

# Disable data layer features — AWS Secret shopxpress-pro/stg/orders-db
# chưa tạo (data layer dev only ở Sub-comp 0.7.5b).
externalSecret:
  enabled: false
migration:
  enabled: false
