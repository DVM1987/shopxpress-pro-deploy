# Dev override — bot auto-bumps image.tag after every successful build-push.

image:
  tag: sha-f807f6c

replicas: 1

# 0.7.5b DONE 2026-05-10: AWS Secret shopxpress-pro/dev/orders-db LIVE
# (TF folder terraform/82-data/), chứa key DATABASE_URL precomputed DSN.
# ESO sync xuống K8s Secret `orders-db-secret`, app pod envFrom pull DSN.
externalSecret:
  enabled: true

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi
